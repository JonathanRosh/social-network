# For running Prisma or the test suite on the host, outside Docker.
# localhost, not postgres, since that's how the container's port is published.
DATABASE_URL=postgresql://social_network:change_me@localhost:5432/social_network
SESSION_SECRET=change_me_too
PORT=4000
